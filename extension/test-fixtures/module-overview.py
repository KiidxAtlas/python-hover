import json
import numpy
import numpy.linalg

# Integration fixture: hover each imported module name to verify that module
# overviews include explanatory prose, not only metadata and export counts.
json.dumps({"ready": True})
numpy.array([1, 2, 3])
numpy.linalg.norm([3, 4])
