[project]
name = "python-hover"
version = "0.7.4"
description = "Python helper utilities for the Python Hover VS Code extension"
requires-python = ">=3.10"
dependencies = []

[tool.uv]
dev-dependencies = [
    "click",
    "fastapi",
    "numpy",
    "pandas",
]
